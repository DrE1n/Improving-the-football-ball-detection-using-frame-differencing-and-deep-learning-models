Here are the files related to the practical part of the bachelor thesis development.

Structure:
1. py. files are related to the applying stabilization methods and frame subtraction to the dataset, training the YOLO10 model and applying it to the video;
2. Results of the model trained on the optimized dataset(graphs, batch samples);
3. Video folder with input and processed football footages. So far, the outputs consist of colored video with and without midpoint logic and a grayscale video.

To be added:
1. Code related to preparing the new dataset, training and applying new YOLO model, a base one without any optimization methods used and a model only using frame differencing without stabilization;
2. New models' results;
3. Respective video footage for the new models

Changes to the existing files are possible